<%= form_with(model: fitness_gym_exercise) do |form| %>
  <% if fitness_gym_exercise.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(fitness_gym_exercise.errors.count, "error") %> prohibited this fitness_gym_exercise from being saved:</h2>

      <ul>
        <% fitness_gym_exercise.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.textarea :description %>
  </div>

  <div>
    <%= form.label :url_video, style: "display: block" %>
    <%= form.url_field :url_video %>
  </div>

  <div>
    <%= form.label :muscle_group, style: "display: block" %>
    <%= form.text_field :muscle_group %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
