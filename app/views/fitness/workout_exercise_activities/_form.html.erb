<%= form_with(model: fitness_workout_exercise_activity) do |form| %>
  <% if fitness_workout_exercise_activity.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(fitness_workout_exercise_activity.errors.count, "error") %> prohibited this fitness_workout_exercise_activity from being saved:</h2>

      <ul>
        <% fitness_workout_exercise_activity.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :workout_activity_id, style: "display: block" %>
    <%= form.text_field :workout_activity_id %>
  </div>

  <div>
    <%= form.label :workout_exercise_id, style: "display: block" %>
    <%= form.text_field :workout_exercise_id %>
  </div>

  <div>
    <%= form.label :gym_exercise_id, style: "display: block" %>
    <%= form.text_field :gym_exercise_id %>
  </div>

  <div>
    <%= form.label :notes, style: "display: block" %>
    <%= form.textarea :notes %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
